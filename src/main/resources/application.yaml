spring:
  datasource:
    username: root
    password: password
    url: jdbc:mysql://localhost:3306/warehouseuse?SSL=false
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
  resources:
    add-mappings: true
server:
  port: 8080
